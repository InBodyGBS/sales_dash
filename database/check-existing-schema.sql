-- Check existing table structure without dropping
-- Run this first to see what columns exist in your current tables

-- Check sales_data table structure
SELECT 
    column_name,
    data_type,
    character_maximum_length,
    is_nullable,
    column_default
FROM information_schema.columns
WHERE table_schema = 'public' 
  AND table_name = 'sales_data'
ORDER BY ordinal_position;

-- Check upload_history table structure
SELECT 
    column_name,
    data_type,
    character_maximum_length,
    is_nullable,
    column_default
FROM information_schema.columns
WHERE table_schema = 'public' 
  AND table_name = 'upload_history'
ORDER BY ordinal_position;

-- Check constraints on sales_data
SELECT 
    conname AS constraint_name,
    contype AS constraint_type,
    pg_get_constraintdef(oid) AS constraint_definition
FROM pg_constraint
WHERE conrelid = 'sales_data'::regclass;
